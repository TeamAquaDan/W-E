import 'package:flutter/material.dart';

class DutchPayPage extends StatelessWidget {
  const DutchPayPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('더치페이'),
      ),
      body: const Column(
        children: [],
      ),
    );
  }
}
